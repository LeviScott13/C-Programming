#include<stdio.h>
#include<string.h>

int main(){

    char words [10];
    while(strcmp(words, "zyzy") != 0) {
    
        printf("Enter a word: ");
        scanf("%s", &words);
    }
    for(int i = 0; i < words; i++){
        
    }
    return 0;
}